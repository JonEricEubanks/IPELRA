# ── Dependencies ─────────────────────────────────────────────
node_modules/

# ── Build output ─────────────────────────────────────────────
app/dist/

# ── Local secrets & settings ─────────────────────────────────
# local.settings.json contains real connection strings and keys locally.
# The committed version is local.settings.json.example (safe placeholder values).
api/local.settings.json
.env
.env.*
*.env

# ── Azure Function deployment artifacts ───────────────────────
funclogs/
*.zip

# ── Generated sponsor QR sheets ───────────────────────────────
# Contains real per-sponsor unlock secrets (?c=...) — never commit to a public repo.
# Regenerate locally with: node scripts/generateQrCodes.js (requires APP_URL + Cosmos connection)
assets/qr-sheets.html
assets/local-test-qr*.png

# ── OS / Editor ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.user
.idea/
.vscode/settings.json

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*
